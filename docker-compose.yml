services:
  postgresql:
    image: postgres:18-alpine
    container_name: sgto-database
    restart: on-failure
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: secure_password
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"